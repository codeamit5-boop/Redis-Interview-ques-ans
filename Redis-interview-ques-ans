1. Redis kya hai?
Redis ek in-memory key-value data store hai jo high performance caching, session management, pub/sub messaging, aur real-time analytics ke liye use hota hai.

2. Redis ka full form kya hai?
Redis ka full form hai:
REmote DIctionary Server.

3. Redis kis tarah ka database hai?
Redis ek NoSQL aur in-memory database hai, jo key-value pair data structure ke form me data store karta hai.

4. Redis disk par data store karta hai ya memory me?
Redis primarily RAM (in-memory) me data store karta hai, lekin persistence ke liye disk par snapshot ya append-only file bhi store kar sakta hai.

5. Redis ka main use-case kya hai?
Caching
Session store
Pub/Sub system
Real-time leaderboard
Message queue

6. Redis kis programming language me likha gaya hai?
Redis C language me likha gaya hai.

7. Redis ka data model kya hai?
Redis key-value store hai, jisme har key ek unique string hoti hai, aur value string, list, hash, set, sorted set, etc. ho sakti hai.

8. Redis me kaunse data types support hote hain?
Strings
Lists
Sets
Sorted Sets (ZSets)
Hashes
Streams
HyperLogLogs
Bitmaps

9. Redis open-source hai?
Haan, Redis open-source hai aur BSD license ke under release hua hai.

10. Redis ek database hai ya cache?
Redis dono kaam karta hai – database bhi hai aur cache bhi.

12. Redis me persistence ke kitne types hote hain?
RDB (Redis Database Backup)
AOF (Append-Only File)

13. RDB kya hai?
RDB Redis ka snapshot mechanism hai jo periodic basis par database ka point-in-time snapshot save karta hai.

14. AOF kya hai?
AOF ek persistence mechanism hai jo har write operation ko log file me append karta hai, taaki Redis restart hone par data restore kiya ja sake.

15. Redis ke liye default port kaunsa hai?
Redis ka default port 6379 hai.

16. Redis ke advantages kya hain?
High performance
In-memory storage
Multiple data structures
Persistence
Pub/Sub messaging

17. Redis me maximum key size kya hai?
Redis me ek key ka maximum size 512 MB ho sakta hai.

18. Redis me ek string value ka maximum size kya hai?
Ek string ka maximum size 512 MB hai.

19. Redis single-threaded hai ya multi-threaded?
Redis ka core execution single-threaded hai, lekin IO handling ke liye Redis multi-threaded ho sakta hai (Redis 6.0+).

20. Redis transactions kya hai?
Redis transactions ek set of commands ko ek saath execute karne ka tarika hai:
Ye commands MULTI se start hote hain aur EXEC par execute hote hain.
Beech me diye gaye saare commands ek queue me store hote hain.
Jab EXEC bola jata hai tab wo atomically run hote hain (yaani ek saath, bina beech me kisi aur operation ke).
Agar DISCARD use karein to queued commands cancel ho jaate hain.

21. MULTI, EXEC, DISCARD commands ka kya kaam hai?
MULTI: Transaction start karta hai
EXEC: Commands execute karta hai
DISCARD: Transaction cancel karta hai

22. Redis replication kya hai?
Replication ek process hai jisme ek Redis server (master) ka data ek ya zyada Redis servers (replica/slave) me copy hota hai.

23. Redis pub/sub kya hai?
Redis Pub/Sub ek messaging system hai jo publishers aur subscribers ke beech real-time messages deliver karta hai.

24. Redis cluster kya hai?
Redis cluster ek distributed setup hai jo multiple Redis nodes par data ko automatically partition karta hai.

25. Redis sentinel kya hai?
Sentinel ek tool hai jo Redis ke high availability ke liye use hota hai. Ye monitoring, notifications aur failover handle karta hai.

26. Redis persistence disable kar sakte hain?
Haan, agar sirf cache ke liye use kar rahe ho to persistence disable kar sakte hain.

27. Redis key expire karne ka command kya hai?
EXPIRE key seconds

28. Redis me TTL kya hai?
TTL (Time To Live) ek key ke expiry time ko define karta hai.

29. Redis me default eviction policy kya hai?
Default policy hai noeviction (agar memory full ho jaaye to new write reject hote hain).

30. Redis me eviction policies ke types kya hain?
noeviction
allkeys-lru
volatile-lru
allkeys-random
volatile-random
volatile-ttl

31. Redis ke commands case-sensitive hote hain?
Nahi, Redis ke commands case-insensitive hote hain.

32. Redis me pipeline kya hai?
Pipeline ek feature hai jo multiple commands ko ek sath server par bhejne ki permission deta hai, jisse performance improve hota hai.

33. Redis ke keys list karne ka command kya hai?
KEYS *

34. Redis ke keys delete karne ka command kya hai?
DEL keyname

35. Redis me sorted set kya hai?
Sorted set ek data structure hai jo unique values ke sath unka score store karta hai, aur unhe score ke basis par sort karta hai.

36. Redis streams kya hain?
Streams ek data structure hai jo log-like message queue implement karta hai aur ordered message delivery ke liye use hota hai.

37. Redis ke memory limit set karne ka command kya hai?
CONFIG SET maxmemory <bytes>

38. Redis ke logs kahan store hote hain?
Default logs /var/log/redis/redis-server.log me store hote hain.

39. Redis me persistence file ke naam kya hain?
RDB file: dump.rdb
AOF file: appendonly.aof

40. Redis ke basic security mechanism kya hai?
Password authentication (requirepass)
Network-level security
ACL (Access Control List)

41. Redis ke liye best use cases kya hain?
Caching
Leaderboards
Session management
Message queues
Real-time analytics

42. Redis ke memory optimization tips kya hain?
Keys chhote rakho
TTL use karo
Eviction policy set karo
Data compression karo

43. Redis ACL kya hai?
ACL (Access Control List) ek security feature hai jo user-level access control provide karta hai.

44. Redis geospatial index kya hai?
Redis me geospatial commands hoti hain jo longitude/latitude ke basis par data store aur query karne ki permission deti hain.

45. Redis ke Lua scripting feature kya hai?
Redis me Lua scripts run kar sakte hain taaki multiple operations ek atomic step me ho sakein.

46. Redis ke client libraries kaunse hain?
Redis ke multiple languages ke liye clients hain:
Java (Jedis, Lettuce), Python (redis-py), Node.js, Go, etc.

47. Redis pub/sub ke real-time use-case kya hain?
Live chat apps
Notifications
Real-time streaming data

48. Redis cluster me data kaise distribute hota hai?
Redis cluster data ko hash slots (0 to 16383) me distribute karta hai.

49. Redis ke monitoring tools kaunse hain?
INFO command
MONITOR command
Redis Sentinel
Grafana, Prometheus

50. Redis ka latest stable version kaise check karenge?
Redis ka latest stable version official website
 ya redis-server --version command se check kar sakte hain.

51. Redis ke “Lazy Deletion” ka concept kya hai?
Lazy deletion me expired keys tabhi delete hoti hain jab unhe access kiya jata hai.

52. Redis ke “Active Expiration” kya hai?
Redis background me randomly keys scan karta hai aur expired keys ko proactively delete karta hai.

53. Redis ke INFO command kya deta hai?
INFO command Redis ke server, clients, memory, replication, persistence ka stats deta hai.

54. Redis ke CLIENT LIST command ka use kya hai?
Ye command connected clients ki list deta hai.

55. Redis ke SLOWLOG kya hai?
Slowlog slow commands ka execution time aur details record karta hai.

56. Redis ke slowlog ka use kya hai?
SLOWLOG GET command se slow queries track kar sakte hain performance tuning ke liye.

57. Redis me hot keys issue kya hai?
Jab ek hi key par bahut zyada traffic hota hai aur performance degrade hota hai.
Solution: Key partitioning ya caching strategy change karo.

59. Redis ke CONFIG command kaise use hota hai?
Runtime configuration changes ke liye, jaise:
CONFIG SET maxmemory 100mb.

60. Redis ke RENAME command kaise use hota hai?
Key rename karne ke liye: RENAME oldKey newKey.

61. Redis ke WATCH command ka kya kaam hai?
WATCH ek key par optimistic lock lagata hai. Agar key change hoti hai to transaction abort hota hai.

62. Redis ke PERSIST command kya karta hai?
Key ka expiry time remove kar deta hai.

63. Redis ke MOVE command kya hai?
Key ko ek database se dusre database me move karta hai.

64. Redis ke MSET aur MGET commands ka kya use hai?
MSET: Multiple keys ek saath set karta hai.
MGET: Multiple keys ek saath get karta hai.

65. Redis list aur set me kya difference hai?
List → ordered collection, duplicates allowed
Set → unordered collection, no duplicates

66. Redis hash ka use kab karte ho?
Key-value pairs ko ek object jaisa store karne ke liye (jaise User object: id, name, email).

67. Redis me sabhi keys kaise dekhte ho?
KEYS * (prod me avoid karein, instead SCAN use karein).

68. Redis को install करने के लिए कौन-सी command use होती है (Linux)?
Ans:
sudo apt install redis-server

69. Redis service को start/stop कैसे करते हैं?
Ans:
Start: sudo service redis-server start
Stop: sudo service redis-server stop

70. Redis में किसी key की value कैसे set करते हैं?
Ans:
SET mykey "Hello"

71. Redis से किसी key की value कैसे get करते हैं?
Ans:
GET mykey

72. Redis में किसी key को delete करने का command क्या है?
Ans:
DEL mykey

73. Redis में किसी key का existence check करने का command क्या है?
Ans:
EXISTS mykey

74. Redis में किसी key का TTL check करने का command क्या है?
Ans:
TTL mykey

75. Redis में किसी key का type check करने का command क्या है?
Ans:
TYPE mykey

76. Redis में list कैसे create करते हैं?
Ans:
LPUSH mylist "A"
LPUSH mylist "B"
LRANGE mylist 0 -1

77. Redis में set कैसे create करते हैं?
Ans:
SADD myset "A"
SADD myset "B"
SMEMBERS myset

78. Redis में hash कैसे create करते हैं?
Ans:
HSET user:1 name "Amit" age "25"
HGETALL user:1

79. Redis में sorted set कैसे create करते हैं?
Ans:
ZADD scores 100 "Alice"
ZADD scores 200 "Bob"
ZRANGE scores 0 -1 WITHSCORES

82. Redis में किसी key की length check कैसे करते हैं?
Ans:
STRLEN mykey

83. Redis में किसी key को expire कैसे करते हैं?
Ans:
EXPIRE mykey 60   # 60 seconds

84. Redis में किसी key को permanent कैसे बनाते हैं (expire हटाना)?
Ans:
PERSIST mykey

85. Redis में किसी key का exact expiry time check करने का command क्या है?
Ans:
PEXPIRETIME mykey

86. Redis में किसी key को increment/decrement कैसे करते हैं?
Ans:
INCR counter
DECR counter

87. Redis में किसी string key में value append कैसे करते हैं?
Ans:
APPEND mykey "World"

88. Redis में किसी key को multiple keys के साथ set करने का तरीका क्या है?
Ans:
MSET key1 "A" key2 "B" key3 "C"

89. Redis में किसी multiple keys की values get कैसे करते हैं?
Ans:
MGET key1 key2 key3

90. Redis में किसी list के last element को निकालने का command क्या है?
Ans:
RPOP mylist

91. Redis में किसी list की length check करने का command क्या है?
Ans:
LLEN mylist

92. Redis में set से कोई random element निकालने का command क्या है?
Ans:
SRANDMEMBER myset

93. Redis में दो sets का intersection कैसे निकालते हैं?
Ans:
SINTER set1 set2

94. Redis में दो sets का union कैसे निकालते हैं?
Ans:
SUNION set1 set2

95. Redis में दो sets का difference कैसे निकालते हैं?
Ans:
SDIFF set1 set2

96. Redis में hash से किसी specific field की value कैसे लेते हैं?
Ans:
HGET user:1 name

97. Redis में किसी hash की सभी fields कैसे निकालते हैं?
Ans:
HKEYS user:1

98. Redis में किसी hash की सभी values कैसे निकालते हैं?
Ans:
HVALS user:1

99. Redis में sorted set का highest score वाला member कैसे निकालते हैं?
Ans:
ZREVRANGE scores 0 0 WITHSCORES

100. Redis में किसी sorted set से score के आधार पर members निकालने का तरीका क्या है?
Ans:
ZRANGEBYSCORE scores 100 200

101. Redis में किसी key को copy करने का तरीका क्या है?
Ans:
COPY sourceKey destinationKey

102. Redis में किसी client connection की info देखने का command क्या है?
Ans:
CLIENT LIST


Redis + Spring Boot Integration

1. Spring Boot application me Redis ka use kyu karte hai?
Answer:
Caching ke liye (fast response ke liye)
Session management
Pub/Sub (messaging system)
Data store as NoSQL DB
Explanation: Redis memory-based DB hai, jo low latency aur high throughput deta hai. Spring Boot ke saath integrate karke performance boost hota hai.

2. Spring Boot me Redis connect karne ke liye kaunsa starter dependency use hota hai?
Answer:
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-redis</artifactId>
</dependency>
Explanation: Ye dependency RedisTemplate, RedisConnectionFactory jaisi classes provide karti hai.

3. RedisConnectionFactory kya hai?
Answer:
RedisConnectionFactory ek Spring Data Redis ka interface hai jo Redis server ke sath connection banane ka kaam karta hai.
Ye RedisTemplate ya StringRedisTemplate ko Redis se connect karne ke liye use hota hai.
👉 Iske implementation jaise LettuceConnectionFactory ya JedisConnectionFactory hote hain.

5. RedisTemplate ka role kya hai?
Answer:
RedisTemplate ek helper class hai jo Redis operations (GET, SET, HASH, LIST, ZSET) perform karne ke liye APIs provide karti hai.
Explanation: Ye abstraction provide karta hai taki aapko low-level Redis command likhne ki zarurat na ho.

6. RedisTemplate me serialization ka kya role hai?
Answer:
Spring me jab hum RedisTemplate use karte hain, tab Serialization ka role bahut important hota hai, kyunki Redis ek byte array store karta hai — yani Redis me data raw bytes ke form me store hota hai.
Aapka Java object directly Redis me store nahi ho sakta.
👉 Isliye Serializer use hota hai jo object ko bytes me convert kare (serialize) aur jab data wapas chahiye ho to bytes se object banaye (deserialize).
Example: StringRedisSerializer, JdkSerializationRedisSerializer, Jackson2JsonRedisSerializer.

7. StringRedisTemplate aur RedisTemplate me difference kya hai?
Answer:
StringRedisTemplate → Keys aur values String ke form me store hoti hai.
RedisTemplate → Generic template jo Object serialization support karta hai.

